import { NoteFormatPipe } from './note-format.pipe';

describe('NoteFormatPipePipe', () => {
  it('create an instance', () => {
    const pipe = new NoteFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
